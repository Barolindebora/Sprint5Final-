<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard de Países</title>

</head>
<body class="dashboard-body">

    <header class="dashboard-header">
        <h1 class="dashboard-title">Países</h1>
    </header>

    <main class="dashboard-main">
        <section class="dashboard-cards-container">
      
            <% paises.forEach(pais => { %>
                <div class="dashboard-card">
                    <h2><%= pais.nombrePais %></h2>
                    <p><strong>Capital:</strong> <%= pais.capital.join(', ') %></p>
                    <p><strong>Limítrofes:</strong> <%= pais.limitrofes.join(', ') || 'N/A' %></p>
                    <p><strong>Área:</strong> <%= pais.area %> km²</p>
                    <p><strong>Población:</strong> <%= pais.poblacion.toLocaleString() %></p>
                    <p><strong>Índice Gini:</strong> <%= pais.gini ?? 'N/A' %></p>
                    <p><strong>Zonas Horarias:</strong> <%= pais.zonaHoraria.join(', ') %></p>
                    <p><strong>Creador:</strong> <%= pais.creador %></p>
                    <div class="card-actions">
                        <a href="/api/formulario/modificar-pais/<%= pais._id %>" class="btn-editar">Editar</a>
                        <form action="/api/paises/borrar/<%= pais._id %>?_method=DELETE" method="POST" onsubmit="return confirm('¿Seguro que querés borrar a <%= pais.nombrePais %>?');">
                            <button type="submit" class="btn-eliminar">Eliminar</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </section>
        <section class="dashboard-resumen">
  <div class="dashboard-card resumen">
    <h2>Resumen</h2>
    <p><strong>Total de Población:</strong> <%= totalPoblacion.toLocaleString() %></p>
    <p><strong>Área Total:</strong> <%= totalArea.toLocaleString() %> km²</p>
    <p><strong>Promedio Índice Gini:</strong> <%= promedioGini %></p>
  </div>
</section>
    </main>

</body>
</html>